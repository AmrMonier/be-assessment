export enum Protocol {
  HTTP = "HTTP",
  HTTPS = "HTTPS",
  TCP = "TCP",
}

export enum Method {
  GET = "GET",
  POST = "POST",
  DELETE = "DELETE",
  PUT = "PUT",
}

export enum Status {
  UP = "UP",
  DOWN = "DOWN",
}

export enum NotificationTypes {
  EMAIL = "email",
}
